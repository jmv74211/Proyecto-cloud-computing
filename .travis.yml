language: python

python:
   - 3.5

install:
   pip install -r requirements.txt

branches:
  only:
  - master

script:
   - python test_funciones_login_registro.py
   - python test_servicio_login_registro.py
